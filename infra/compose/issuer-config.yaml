issuer: http://mock-issuer:8088
audience: collectmind-api
key_id: dev-key-1
clients:
  - client_id: feature-001-default
    client_secret: local-dev-only
    tenant_id: feature-001-default
    scopes: [findings:write, findings:read]
  # Feature 002: two additional tenants for the multi-tenant negative-path test bar.
  - client_id: tenant-a
    client_secret: local-dev-only
    tenant_id: tenant-a
    scopes: [findings:write, findings:read]
  - client_id: tenant-b
    client_secret: local-dev-only
    tenant_id: tenant-b
    scopes: [findings:write, findings:read]
